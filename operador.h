#pragma once

#include <bits/stdc++.h>
#include "arbol.h"
using namespace std;

//el enum de los operadores esta en pruebas 

class Operador
{
private:
    char oper;
public:
    Operador(char oper);
    char getOper();
    
};
